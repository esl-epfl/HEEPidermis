// Copyright 2025 EPFL contributors
// Solderpad Hardware License, Version 2.1, see LICENSE.md for details.
// SPDX-License-Identifier: Apache-2.0 WITH SHL-2.1
//
// File: vREF-waivers.vlt
// Author: David Mallasen
// Description: Verilator waivers for vREF

`verilator_config
lint_off -rule WIDTH -file "*Verilog/vREF.sv" -match "Operator * expects * bits on the *, but * generates * bits."
lint_off -rule UNUSED -file "*Verilog/vREF.sv" -match "Bits of signal are not used: *"
lint_off -rule BLKSEQ -file "*Verilog/vREF.sv" -match "*"
lint_off -rule LATCH -file "*Verilog/vREF.sv" -match "*Latch inferred for signal*"
